const computingProcesses_a = prompt ("Первое число", 2);
const computingProcesses_b = prompt ("Второе число", 2);

alert (`${computingProcesses_a} + ${computingProcesses_b} = 4 
${computingProcesses_a} - ${computingProcesses_b} = 0
${computingProcesses_a} * ${computingProcesses_b} = 4
${computingProcesses_a} / ${computingProcesses_b} = 1`);
